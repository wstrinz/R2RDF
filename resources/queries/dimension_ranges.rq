PREFIX qb:   <http://purl.org/linked-data/cube#> 
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#> 

SELECT ?dimension ?range WHERE {
	?dim a qb:DimensionProperty;
		rdfs:label ?dimension;
		rdfs:range ?range.
}